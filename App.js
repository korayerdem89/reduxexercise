import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

const initialState = {
counter:0,

};


export default () => {
  return (
    <SafeAreaView>
      <Text>Hello Redux</Text>
    </SafeAreaView>
  );
};

const First = () => {
return (

);
};
